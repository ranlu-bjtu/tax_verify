# 运营使用手册

本项目已提供本地运营工作台，日常批量取数和验证优先使用页面操作，不需要手动拼命令。

## 启动工作台

在项目根目录执行：

```powershell
python scripts\ops_console.py --open
```

默认地址：

```text
http://127.0.0.1:8765/
```

如果 8765 端口被占用，程序会自动尝试后续端口，终端会打印实际地址。

## 运行前检查

打开页面后先看“环境检查”：

- 浏览器连接：完整链路需要 Chrome CDP 正常。
- 税局插件：需要能找到 EtaxPlugin。
- 易代账凭据：如果浏览器已有登录态，可以不填；如果登录态失效，在页面“任务参数”里临时填写易代账账号和密码即可。
- 代理：税局异常时优先关闭代理。
- 运行任务：同一时间只建议跑一个批量任务。

## 发起完整链路

1. 任务类型选择“完整链路：发取数并验证”。
2. 填写所属期，例如 `202604`。
3. 企业默认 `蓝天之爱`。
4. 如果易代账没有登录态，填写易代账账号和密码。账号密码只传给本次后台任务，不写入任务记录。
5. 税号清单每行一个，也支持逗号分隔。
6. 点击“开始任务”。

页面会展示当前批次、运行状态、日志尾部和最近批次。

## 查看税号进度

工作台会在“税号进度”中按税号展示当前阶段：

- 发起取数
- 等待取数完成
- 查询 taskId
- 验证数据
- 完成验证
- 需人工处理

如果某个税号卡住，优先看“原因”和“操作”两列。

## 续跑和单税号处理

- 继续未完成：对当前批次中未完成的税号继续执行。
- 继续验证：只对当前税号使用已有 taskId 重新验证，不重新发起取数。
- 重试取数：只对当前税号重新发起取数，并在获取 taskId 后继续验证。
- 跳过：把当前税号标记为运营手动跳过，并重新生成汇总页。
- 重新生成汇总页：不重新跑任务，只根据当前状态文件刷新 HTML 报告。

停止任务会尝试结束批量脚本及其子进程，但不会主动关闭 Chrome。

## 常用选项

- 强制重新发起取数：账套已经取过但需要重跑时勾选。
- 已验证也重跑：已有验证结果但需要覆盖重跑时勾选。
- 跳过税局浏览器验证：只验证接口和映射，不进入税局页面。
- 不保存 PDF：需要加快验证且不需要留存 PDF 时勾选。

## 查看结果

- 点击“打开最新报告”可以打开最近一次批量汇总页。
- 最近批次列表中点击“打开”可以查看指定批次。
- 汇总页中的“需处理原因”只展示最直接的人工处理原因。
- 差异明细按“表单 / 账户 / 字段”分组展示。

## 问题处理

工作台的“问题处理”区域会读取当前批次的字段差异明细，并合并运营处理记录。

可操作内容：

- 修改处理状态：待处理、处理中、已确认接口问题、已确认网页解析问题、已确认税局数据问题、已忽略、已完成。
- 填写处理备注。
- 打开证据：报告、PDF、接口填充 Excel。
- 导出问题清单：生成 `output/batch_runs/<runId>/exports/问题处理清单_<runId>.xlsx`。

处理记录写入：

```text
output/batch_runs/<runId>/ops_review.json
```

该文件只保存处理状态和备注，不保存账号、密码、cookie 或 token。

## 仍需命令行时

工作台底层仍调用：

```powershell
python scripts\batch_collect_verify.py ...
```

真实 taskId 单次验证仍使用：

```powershell
python main.py --task-id <taskId> --log-level INFO
```

## 覆盖检查

工作台的“覆盖检查”会按项目当前支持的税种，展示“已申报 / 未申报”两种状态是否已经有代表样本。

当前覆盖目标包括：

- 增值税（一般纳税人）
- 增值税（小规模纳税人）
- 企业所得税（A类）
- 文化事业建设费
- 残保金

每次批量汇总会生成：

```text
output/batch_runs/<runId>/coverage_status.json
output/batch_runs/<runId>/coverage_matrix.csv
```

如果存在缺口，后续会按后台成功取数任务补齐代表 taskId；目前后台任务结果 JSON 中“申报状态”字段尚未最终确认，补齐逻辑先保留为框架。
